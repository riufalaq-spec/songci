# Project: 宋词别苑 (Song Ci Pavilion) - Enterprise Specification

## 1. 项目愿景
打造一个兼具中国传统美学与现代高可用架构的宋词沉浸式平台。引入微服务架构与用户账户体系，支持全量宋词的智能检索、个性化雅集（收藏/点赞）以及文人墨客社交化归档。

## 2. 技术栈与架构 (Architecture & Tech Stack)
- **Frontend**: React (Vite) + Tailwind CSS
- **Backend (Microservices)**: Go + Gin (API Gateway/HTTP Router) + Go-Micro (RPC Microservices)
- **Data & Cache**: MySQL (持久化用户、词作、诗人、收藏表) + Redis (用于用户 Session/Token、搜索缓存、点赞计数器)

### 微服务拆分预留：
1. `User Service`: 处理邮箱注册、登录、密码重置、JWT 鉴权。
2. `Poetry Service`: 处理宋词检索、三百首精选、诗人档案、横竖排版元数据。
3. `Interaction Service`: 处理高并发的点赞（Redis 计数异步落库）、收藏夹（MySQL）。

---

## 3. 增强版页面规划与功能矩阵

### 页面 1：首页 / 智能检索与精选页 (Home, Search & Three Hundred)
- **视觉**: 极简宣纸风。
- **功能组件**:
  1. **全局智能搜索框**: 输入时触发防抖（Debounce）请求后端，在**搜索框下方弹出悬浮结果面板（Popover）**，分栏展示“相关词牌”、“相关诗人”、“内容片段”。
  2. **宋词三百首入口**: 点击跳转至**专属二级独立页面**。该页面以卡片流形式展示 300 首精选词作，支持按热度/作者快速过滤。

### 页面 2：词作详情 / 沉浸式阅读页 (Ci Detail)
- **功能组件**:
  1. **排版切换引擎**: 支持一键切换“横排（Modern）”与“竖排（Classical）”。
  2. **竖排模式设计**: 
     - 容器宽度自适应，内容整体靠右对齐。
     - 文本行从右向左流动（`writing-mode: vertical-rl`），字间距加宽。
     - 底部互动栏（点赞/收藏）变为左侧或右侧的悬浮挂件。

### 页面 3：文人墨客 / 诗人主页 (Author Domain)
- **状态 A：诗人列表流（默认）**:
  - 包含诗人模糊搜索框。
  - 网格化展示常见/热门诗人卡片（预留头像、姓名、时代、代表作摘要）。
- **状态 B：诗人详情（选中后）**:
  - 点击列表卡片无缝进入详情。
  - 左侧/顶部展示复杂的诗人背景（支持后续扩展生卒年、地理位置、图片）。
  - 右侧展示其作品归档。
  - **核心交互**: 提供显眼的“返回列表”按钮，点击后恢复状态 A，且**保持之前的搜索词和滚动位置**。

### 页面 4：个人雅集 / 用户中心 (Collection & Auth Gateway)
- **场景 A：未登录状态（鉴权拦截）**:
  - 展示唯美背景的占位提示：“登录后方可开启个人雅集，收录您的挚爱词作”。
  - 提供醒目的“去登录/注册”引导按钮。
- **场景 B：已登录状态**:
  - 展示多 Tab 选项卡（我的收藏、我的点赞、浏览历史）。
  - 卡片支持一键取消收藏，数据异步同步至后端 Interaction Service。

### 页面 5：账号中心独立页 (Identity & Access Management)
- **子模块（支持单页 Tab 切换或独立路由）**:
  1. **邮箱登录**: 邮箱+密码，包含表单验证、登录失败提示、以及“忘记密码”入口。
  2. **邮箱注册**: 邮箱 + 密码 + 确认密码，支持发送验证码（预留接口）。
  3. **忘记密码**: 输入注册邮箱 -> 发送重置链接/验证码 -> 设置新密码。

---

## 4. Claude Code 实施指南
1. **API 优先**: 优先定义前端与 Gin 网格的 RESTful API 契约（如 `/api/v1/auth/login`, `/api/v1/poetry/search`）。
2. **UI 健壮性**: 在编写前端组件时，必须完整实现 Loading 骨架屏、后端报错的 Toast 提示以及 Token 过期自动弹窗登录的拦截器逻辑。